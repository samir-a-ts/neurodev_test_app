name: neurodev_test_app
description: Test app for Neurodev.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Dart utils
  equatable: ^2.0.5
  
  # State management
  get_it: ^7.6.0
  flutter_bloc: ^8.1.3
  bloc: ^8.1.2

  # Network
  dio: ^5.1.2

  # UI
  flag: ^6.0.0

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
